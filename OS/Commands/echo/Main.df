#include "<df>/dragonfruit.h"

#include "<ll>/OSDLL/OS.h"

fn Main { ... -- ret }
	0 ret!

	auto i
	1 i!

	while (i@ argc@ <)
		[i@]argv@ "%s" Printf

		if (i@ argc@ 1 - ~=)
			' ' Putc
		end else
			'\n' Putc
		end

		1 i +=
	end
end