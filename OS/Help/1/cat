[7m                     MINTIA Operating System: Online Manual                    [m

NAME
    cat -- concatenate and print files

SYNOPSIS
    cat [-u] [file ...]

DESCRIPTION
    cat sequentially reads each specified file in the order provided and writes
    their contents to standard output. If no filenames are provided, cat reads
    from standard input.

    The switches are as follows:

    -u      Open the files noncached.
