[7m                     MINTIA Operating System: On-line Manual                   [m

NAME
    killall -- send signal to processes by name

SYNOPSIS
    killall [-SIGNAL] name

DESCRIPTION
    killall sends the specified signal to all processes with the given name
    to which the calling user has ACCESS_EXEC permissions. If no signal is
    specified, then SIGTERM is sent by default. The signal may be specified as
    a numeric signal number or as a symbolic name. For all of the symbolic names
    for signals, see (1)kill.

SEE ALSO
    (1)kill
