#include "<df>/dragonfruit.h"
#include "<inc>/HALConsole.h"

fn FPuts { fd s -- }
	while (s@ gb)
		s@ gb Putc
		1 s +=
	end
end

fn FPutc { fd c -- }
	c@ Putc
end

fn Putc { c -- }
	c@ HALPutc
end

fn Malloc { -- } end
fn Free { -- } end
fn Calloc { -- } end